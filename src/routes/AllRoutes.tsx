import ReactDOM from "react-dom/client";
import { BrowserRouter, Routes, Route } from "react-router-dom";

const AllRoutes = () => {
  return (
    <BrowserRouter>
      <Route></Route>
    </BrowserRouter>
  );
};
